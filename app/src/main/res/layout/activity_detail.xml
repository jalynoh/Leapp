<?xml version="1.0" encoding="utf-8"?>


<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="16dp"
    android:paddingLeft="16dp"
    android:paddingRight="16dp"
    android:paddingTop="16dp"
    tools:context="com.example.zaimzanaruddin.derplist.ui.DetailActivity">

    <TextView
        android:id="@+id/lbl_quote_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-medium" />

    <TextView
        android:id="@+id/lbl_quote_loc"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/lbl_quote_title"
        android:fontFamily="sans-serif-light"
        android:textStyle="italic" />

    <TextView
        android:id="@+id/lbl_quote_date"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/lbl_quote_loc"
        android:fontFamily="sans-serif-light"
        android:textStyle="italic" />

    <TextView
        android:id="@+id/lbl_quote_time"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/lbl_quote_date"
        android:fontFamily="sans-serif-light"
        android:textStyle="italic" />

</RelativeLayout>
